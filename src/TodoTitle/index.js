/**
 * TodoTitle
 *
 * Displays the main title of the application.
 *
 * @returns {JSX.Element} A heading with the app title.
 */
function TodoTitle() {
    return (
        <h1>To-Do List</h1>
    )
}

export { TodoTitle };